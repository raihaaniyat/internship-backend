# Optional overrides for the browser (must start with NEXT_PUBLIC_).
# Use the SAME host you type in the address bar: if you open
# http://127.0.0.1:3002 use 127.0.0.1 below, not localhost.
# NEXT_PUBLIC_AUTH_BASE_URL=http://localhost:3001
# NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
